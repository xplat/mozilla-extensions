We can probably accomplish this by reusing the load context (viewer-load-context.js) and have it follow the content object throughout its lifecycle.  Content objects would be required to add event handlers through an interface on this object and the handlers would be stored.  If they weren't removed by the time `surrender` or `cleanup` were called they would be cleaned up automatically.

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "viewer_host_utils"
version = "0.1.0"
requires-python = ">=3.8"
description = "Shared utilities for Mozilla extension native messaging hosts"

[project.optional-dependencies]
dev = []

'use strict';
// ── viewer-media-imagelike.js ─────────────────────────────────────────────────
//
// Shared base class for image and gif-loop content occupants.
// A stub for now; will fill out when per-occupant event handlers are added.
//
// Declares these globals used by other modules:
//   ImagelikeContent.
//
// Calls into globals defined in earlier / later modules:
//   ContentOccupant.                                        (viewer-media.js)

class ImagelikeContent extends ContentOccupant {
  // Just a stub at this point, but will fill out when occupant event handlers
  // are a thing.
}
